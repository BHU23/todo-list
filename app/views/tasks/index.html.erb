   <%= turbo_frame_tag "flash_notice" do %>
      <%= render "flash" %>
  <% end %>
  
 <%= turbo_frame_tag 'task_pages' do %>
 <div class="container min-h-screen flex flex-wrap items-start justify-center gap-4">
  <div class="min-w-full lg:min-w-[40rem] p-10 rounded shadow-lg">
    <h1 class="text-4xl font-bold mb-4 text-center text-blue-500">Todos</h1>
    
    <%= turbo_frame_tag 'new_task_form' do %>
      <%= render "form", task: @task %> 
    <% end %>
  </div>

  <div class="max-w-sm min-w-full lg:min-w-[30rem] h-auto min-h-[45rem] flex flex-col rounded shadow-lg p-10">
    <h2 class="text-xl font-bold mb-4 text-blue-500">Tasks</h2>
    <%# <div class="flex gap-2 mb-4">
      <%= link_to 'Show All', tasks_path(filter: 'all'), remote: true, class: "w-1/3 #{params[:filter] == 'all' ? 'bg-blue-500' : 'bg-gray-300 hover:bg-gray-400'} text-white px-4 py-2 rounded text-center" %>
      
      <%# <%= link_to 'Completed', tasks_path(filter: 'completed'), remote: true, class: "w-1/3 #{params[:filter] == 'completed' ? 'bg-blue-500' : 'bg-gray-300 hover:bg-gray-400'} text-white px-4 py-2 rounded text-center" %>
      
      <%# <%= link_to 'Uncompleted', tasks_path(filter: 'uncompleted'), remote: true, class: "w-1/3 #{params[:filter] == 'uncompleted' ? 'bg-blue-500' : 'bg-gray-300 hover:bg-gray-400'} text-white px-4 py-2 rounded text-center" %> 
    <%# </div> %> 
    <div class="flex gap-2 mb-4">
      <%= link_to 'Show All', tasks_path(filter: 'all'), remote: true, class: "w-1/3 #{params[:filter] == 'all' ? 'border-blue-600 text-blue-600' : 'border-gray-400 text-gray-600 hover:border-b-2 hover:border-blue-300'} border-b-2 border-0 px-4 py-2 text-center", data: { top: true } %>

      <%= link_to 'Completed', tasks_path(filter: 'completed'), remote: true, class: "w-1/3 #{params[:filter] == 'completed' ? 'border-blue-600 text-blue-600' : 'border-gray-400 text-gray-600 hover:border-b-2 hover:border-blue-300'} border-b-2 border-0 px-4 py-2 text-center", data: { top: true } %>

      <%= link_to 'Uncompleted', tasks_path(filter: 'uncompleted'), remote: true, class: "w-1/3 #{params[:filter] == 'uncompleted' ? 'border-blue-600 text-blue-600' : 'border-gray-400 text-gray-600 hover:border-b-2 hover:border-blue-300'} border-b-2 border-0 px-4 py-2 text-center", data: { top: true } %>
    </div>

    <h3 class="text-lg font-bold text-green-500 mt-6">Tasks List</h3>
    <%= turbo_frame_tag 'tasks' do %>
      <%= render partial: "task", collection: @tasks, as: :task %>
    <% end %>
  </div>

</div>

<% end %>