<%= turbo_stream.replace "new_task_form" do %>
  <%= render "form", task: Task.new %> 
<% end %>

<%= turbo_stream.append "tasks" do %>
  <%= render @task %> <!-- Assuming you have a partial for each task, e.g., _task.html.erb -->
<% end %>

<%= turbo_stream.append "flash_notice" do %>
  <%= render "flash", notice: flash[:notice], status: flash[:status] %>
<% end %>

<%= turbo_stream.replace "flash_notice" , partial "layouts/flash" , locals: {noticez: "test jaaaa", status: "success" }%>
